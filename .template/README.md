# Project Skeleton

This directory contains the skeleton files for initializing an MCP server using the justfile command `just create-server <server>`.

`fastmcp.json` and `main.py` are copied to your 