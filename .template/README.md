# Project Skeleton

This directory provides the template files used when creating a new project in this workspace with the command `just create-server <server>`.

`fastmcp.json` and `main.py` are copied during initialization.

You can modify these template files to change the defaults for new servers in your project.
