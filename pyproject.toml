[project]
name = "prefect-fastmcp-demos"
version = "0.1.0"
requires-python = ">=3.12"
readme = "README.md"
dependencies = [
    "fastmcp==2.13.0.2",
    "pydantic==2.12.4",
]

[tool.uv.workspace]
members = ["servers/*"]
