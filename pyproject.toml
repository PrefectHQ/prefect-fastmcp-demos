[project]
name = "prefect-fastmcp-demos"
version = "0.1.0"
requires-python = ">=3.12"
readme = "README.md"
dependencies = [
    "fastmcp",
]

[tool.uv]
constraint-dependencies = [
    "fastmcp>=2.12.0"
]

[tool.uv.workspace]
members = ["servers/*"]
